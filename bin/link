#! /bin/bash

rm -f ~/.tmux.conf
rm -f ~/.vimrc
rm -f ~/.env-var
rm -f ~/.zshrc

ln -sf $PWD/tmux.conf.dot ~/.tmux.conf
ln -sf $PWD/vimrc.dot ~/.vimrc
ln -sf $PWD/env-var.dot ~/.env-var
ln -sf $PWD/zshrc.dot ~/.zshrc
